name: Django Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:10.8
        env:
          POSTGRES_USER: mkdnkpathkkuvj
          POSTGRES_PASSWORD: 78fd400419ef1a8fe37b365d650b3fce21f7a733b5f44de35ecfc96c9347d327
          POSTGRES_DB: d88g1f7fkvu6j6
        ports:
          - 5432:5432
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.8]

    steps:
    - uses: actions/checkout@v2
    - run: |
        git fetch --prune --unshallow
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        cd warranty_service/
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Migrate
      run: |
        cd warranty_service/
        python3 manage.py makemigrations
        python3 manage.py migrate
    - name: Deploy to Heroku
      uses: akhileshns/heroku-deploy@v3.4.6
        with:
          heroku_api_key: 9bb8f29a-3dab-4878-86a3-33ab7960a1cb
          heroku_app_name: warranty-ivan
          heroku_email: Chernov@mail.ru
          appdir: warranty_service

